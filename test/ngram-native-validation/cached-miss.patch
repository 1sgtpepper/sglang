diff --git a/python/sglang/kernels/jit/csrc/ngram_corpus/trie.cpp b/python/sglang/kernels/jit/csrc/ngram_corpus/trie.cpp
index cc05a016a..c5859f7ac 100644
--- a/python/sglang/kernels/jit/csrc/ngram_corpus/trie.cpp
+++ b/python/sglang/kernels/jit/csrc/ngram_corpus/trie.cpp
@@ -39,0 +40 @@ void Trie::insert(const int32_t* tokens, size_t len) {
+        ++growth_epoch_;
@@ -120,0 +122,5 @@ bool Trie::validateMatchState_(const MatchState& state) const {
+    // A new edge can make a previously missing suffix matchable without
+    // changing the version of any live cached node.
+    if (ref.ptr == nullptr && state.growth_epoch != growth_epoch_) {
+      return false;
+    }
@@ -130,0 +137 @@ void Trie::rebuildMatchState_(const int32_t* context, size_t len, MatchState& st
+  state.growth_epoch = growth_epoch_;
@@ -188,0 +196 @@ bool Trie::advanceMatchState_(MatchState& state, const int32_t* tokens, size_t l
+  state.growth_epoch = growth_epoch_;
diff --git a/python/sglang/kernels/jit/csrc/ngram_corpus/trie.h b/python/sglang/kernels/jit/csrc/ngram_corpus/trie.h
index 4fa58c4c2..a7ef088ab 100644
--- a/python/sglang/kernels/jit/csrc/ngram_corpus/trie.h
+++ b/python/sglang/kernels/jit/csrc/ngram_corpus/trie.h
@@ -53,0 +54,2 @@ struct MatchState {
+  // Cached misses remain valid only while the trie has not grown.
+  uint64_t growth_epoch = 0;
@@ -141,0 +144 @@ class Trie {
+  uint64_t growth_epoch_ = 1;
